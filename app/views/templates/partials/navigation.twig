<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="#">SDMatehuala</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li><a href="{{ urlFor('home') }}">Inicio<span class="sr-only">(current)</span></a></li>
        <li><a href="{{ urlFor('expenses') }}">Gastos</a></li>
        <li><a href="{{ urlFor('category') }}">Categorias</a></li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li><a href="{{ urlFor('expenses-upload') }}">+ XML</a></li>
        <li><a href="{{ urlFor('expenses-add') }}">+ Gasto</a></li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Reportes<span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><a href="{{ urlFor('expenses-report') }}">Reporte Gastos</a></li>
            <li><a href="{{ urlFor('tax-report') }}">Reporte Impuestos</a></li>
          </ul>
        </li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><img src="{{ auth.getAvatarUrl({size:20}) }}" width="20"> {{ auth.getFullNameOrUsername }} <span class="caret"></span></a>
          <ul class="dropdown-menu">
           <li><a href="{{ urlFor('user.profile', {username: auth.username}) }}">Cuenta</a></li>
           <li><a href="{{ urlFor('settings') }}">Ajustes</a></li>
            <li><a href="{{ urlFor('auth.password.change') }}">Cambiar password</a></li>
            <li role="separator" class="divider"></li>
            <li><a href="{{ urlFor('logout') }}">Logout</a></li>
          </ul>
        </li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>